# Kanvis Edge — solo DHCP en el AP (sin DNS; --port=0 en la línea de comandos)
interface=@WLAN_INTERFACE@
bind-interfaces
except-interface=lo
dhcp-range=@DHCP_START@,@DHCP_END@,12h
dhcp-option=3,@AP_IP@
