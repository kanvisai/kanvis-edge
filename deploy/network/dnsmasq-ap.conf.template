# Kanvis Edge — solo DHCP en el AP (sin servidor DNS en :53)
interface=@WLAN_INTERFACE@
bind-interfaces
except-interface=lo
port=0
dhcp-range=@DHCP_START@,@DHCP_END@,12h
dhcp-option=option:router,@AP_IP@
log-dhcp
